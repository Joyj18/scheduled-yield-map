[project]
name = "scheduled-yield-map"
authors = ["Your Name"]
description = "A decentralized productivity tracking and goal management system"
telemetry = false
requirements = []
[contracts.yield-mapper]
path = "contracts/yield-mapper.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
